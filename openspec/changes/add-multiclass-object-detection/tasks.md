## 1. Multi-class detection core (`NBA.Vision`)

- [x] 1.1 Add `OnCourtObjectDetection` (readonly record struct: `Left`, `Top`, `Right`, `Bottom`, `Confidence`, `ClassName`) in `src/NBA.Vision/OnCourtObjectDetection.cs`. Verify the solution builds.
- [x] 1.2 Add `MultiClassDetectionResult` (record: `IReadOnlyList<PlayerDetection> Players`, `IReadOnlyList<OnCourtObjectDetection> Others`) and `IMultiClassObjectDetector` (single `Detect(ReadOnlySpan<byte> bgra8Pixels, int width, int height, int stride) -> MultiClassDetectionResult`, mirroring `IPlayerDetector`'s pixel-span parameter shape) in `src/NBA.Vision/MultiClassDetectionResult.cs` / `src/NBA.Vision/IMultiClassObjectDetector.cs`. Verify the solution builds.
- [x] 1.3 Add `NullMultiClassObjectDetector : IMultiClassObjectDetector` in `src/NBA.Vision/NullMultiClassObjectDetector.cs`, always returning `Players: []`, `Others: []`, mirroring `NullPlayerDetector`. Verify with a unit test asserting it always returns empty regardless of input.
- [x] 1.4 Rename `OnnxPlayerDetector` to `OnnxMultiClassObjectDetector` (`src/NBA.Vision/OnnxMultiClassObjectDetector.cs`), implementing `IMultiClassObjectDetector` instead of `IPlayerDetector`. Add a `classNames: IReadOnlyList<string>` constructor parameter (caller-supplied, per design.md's "never hardcoded/guessed" decision). Rewrite postprocessing to loop over every class channel (`4 + classIndex` for `classIndex` in `0..classNames.Count`): collect above-threshold candidates per channel, run the existing `SuppressOverlapping` NMS independently within each channel, then route survivors of the `playerClassId` channel through the existing upper-body-color-sampling path into `PlayerDetection` and every other channel's survivors into `OnCourtObjectDetection` (using `classNames[classIndex]`). Preserve the existing out-of-range `playerClassId` guard (extended to also guard `classNames.Count` against the model's actual channel count). Verify the solution builds.
- [x] 1.5 Update `tests/NBA.Vision.Tests/OnnxPlayerDetectorTests.cs` (rename to `OnnxMultiClassObjectDetectorTests.cs`) to construct `OnnxMultiClassObjectDetector` with a `classNames` list matching the existing 2-class fixture (e.g. `["Other", "Player"]` or similar, matching whichever index `playerClassId` already pointed at). Verify every existing test still passes unmodified in behavior (this is the `Player`-path regression guard - same inputs must produce the exact same `PlayerDetection` results as before this change).
- [x] 1.6 Add unit tests covering the new multi-class behavior: a candidate above threshold on a non-`Player` channel produces an `OnCourtObjectDetection` with the correct `ClassName`; two heavily-overlapping candidates on the *same* non-`Player` channel are suppressed to one (per-class NMS); two heavily-overlapping candidates on *different* channels (including one being the `Player` channel) are NOT suppressed against each other; a frame with detections on multiple channels returns them correctly split between `Players` and `Others`.

## 2. App wiring

- [x] 2.1 In `src/NBA.App/App.axaml.cs`, replace the `OnnxPlayerDetector`/`NullPlayerDetector` construction with `OnnxMultiClassObjectDetector`/`NullMultiClassObjectDetector`, passing `classNames` as the verified 9-entry list from `models/README.md`'s embedded metadata (`["Ball", "Hoop", "Period", "Player", "Ref", "Shot Clock", "Team Name", "Team Points", "Time Remaining"]`) and keeping the existing `playerClassId: 3`, `inputSize: 1280`, `confidenceThreshold`, `iouThreshold` arguments unchanged. Verify the solution builds and the app still starts.
- [x] 2.2 In `src/NBA.App/ViewModels/MainWindowViewModel.cs`, change the constructor parameter and backing field from `IPlayerDetector _playerDetector` to `IMultiClassObjectDetector _multiClassObjectDetector`. In `ProcessFrameArrived`'s detection-frame branch (~line 218-239), replace the `_playerDetector.Detect(...)` call with one `_multiClassObjectDetector.Detect(...)` call, feed `.Players` (after the existing `OffsetToFullFrame` mapping, unchanged) into `_playerTracker.Update(...)` exactly as today, and keep the result's `.Others` available for tasks 3 and 4 below. Verify the solution builds.
- [x] 2.3 Update `tests/NBA.App.Tests`' `StubPlayerDetector` to a `StubMultiClassObjectDetector : IMultiClassObjectDetector` (returning a configurable `MultiClassDetectionResult`), and update every `MainWindowViewModelTests` construction site accordingly. Verify all existing `MainWindowViewModelTests` pass unmodified in behavior.
- [x] 2.4 Add a `MainWindowViewModelTests` case asserting a detection frame triggers exactly one `Detect(...)` call on the stub (not one for players and a separate one for other classes) - the regression guard for this change's core "one inference pass" goal.

## 3. Overlay rendering of non-Player classes

- [x] 3.1 In `MainWindowViewModel.ProcessFrameArrived`, build one `OverlayAnnotation.ForBox(...)` per detection in `.Others` (label and `styleKey` both the detection's `ClassName`), and concatenate it into every `RawOverlay.SetAnnotations(...)` call alongside `playerAnnotations` (the early-return branch at ~line 283-291, the OCR-throttle branch, and the calibrated-minimap branch at ~line 344/408). Verify with a `MainWindowViewModelTests` case asserting a frame with non-`Player` detections produces one additional `OverlayAnnotation` per detection with the expected label/`styleKey`.
- [x] 3.2 In `src/NBA.App/Views/RawOverlayView.axaml.cs`, add a small `ClassName -> Brush` lookup (one distinct color per non-`Player` class) and extend `CreateControls(AnnotationVisual)`'s box branch to use it when `StyleKey` matches a known class name, falling back to the existing fixed `#FF4040` otherwise (preserving today's look for tracked-player boxes, whose `StyleKey` stays unset, and for anything unrecognized). Verify by running the app against a source with detections and visually confirming non-`Player` classes render in distinct colors with class-name labels (no automated UI test harness exists for this, per the precedent set in `add-player-minimap-projection`).

## 4. Scoreboard OCR crop-region sourcing

- [x] 4.1 In `MainWindowViewModel`, add a per-source `_lastScoreboardObjectRegion: NormalizedRect?` field, reset to `null` alongside the other per-source state already reset on source switch. On every detection frame, filter `.Others` to the five scoreboard-related class names (`Period`, `Shot Clock`, `Team Name`, `Team Points`, `Time Remaining`); if any survive, compute their union bounding box in image-pixel space, normalize it against the same reference frame `NormalizedRect.DefaultScoreboardRegion`/`SourceProfile.ScoreboardRegion` already use (the playback-region crop once known, else the full frame), and overwrite `_lastScoreboardObjectRegion`; if none survive, leave the previously cached value untouched. Verify the solution builds.
- [x] 4.2 In the scoreboard-OCR block (~line 300-312), change the crop-region selection to: `_profileStore.Load(sourceKey)?.ScoreboardRegion ?? _lastScoreboardObjectRegion ?? NormalizedRect.DefaultScoreboardRegion` (manual override still wins outright, per design.md). Verify with unit tests (a small pure-function extraction of this precedence logic is acceptable if that's easier to test than the full OCR block): detected region is used when present and no manual override is set; manual override wins even when a detected region is also present; default fallback is used when neither is present.
- [x] 4.3 Add a `MainWindowViewModelTests` case asserting the union bounding box of multiple scoreboard-related-class detections (e.g. `Period` and `Time Remaining` boxes at different positions) is computed correctly (covers at least the union's min/max edges).

## 5. Documentation & validation

- [x] 5.1 Update `models/README.md`'s `player-detection.onnx` section to note that all 9 classes are now decoded from every inference pass, not just `Player` (the earlier "only `Player`/`Ref` are read, others are deliberately never read" framing from `add-team-color-track-gating` no longer applies to `Ref`/other classes' *detection*, only to what consumes them downstream).
- [x] 5.2 Run the full solution test suite (`dotnet test NBA.slnx`) and confirm all new and existing tests pass, with particular attention to `tracking/player-tracking` and team-color-gating tests passing unmodified (proving the `Player`-path regression guard holds end-to-end, not just at the `NBA.Vision` unit level).
- [ ] 5.3 Run the app against a real or recorded broadcast clip; confirm non-`Player` classes render as labeled boxes distinct from tracked-player boxes, and observe whether scoreboard OCR reads remain correct (or improve) using the detected crop region compared to the previous fixed-region behavior.
