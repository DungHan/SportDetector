## 1. Test project scaffolding & core types

- [x] 1.1 Create `tests/NBA.Tracking.Tests/NBA.Tracking.Tests.csproj`, mirroring `tests/NBA.Vision.Tests/NBA.Vision.Tests.csproj` (xunit + coverlet package references, `Using Include="Xunit"`), with `ProjectReference`s to `src/NBA.Tracking/NBA.Tracking.csproj` and `src/NBA.Vision/NBA.Vision.csproj` (needed for `PlayerDetection`) — no `Assets/` fixture wiring, since this change loads no model file. Add the project to `NBA.slnx` under `/tests/`. Verify `dotnet test tests/NBA.Tracking.Tests` runs cleanly (zero tests yet, exit code 0).
- [x] 1.2 Define `TrackedPlayer` (readonly record struct: `TrackId` (int), `Left`/`Top`/`Right`/`Bottom` (double), `Confidence` (float)) in `src/NBA.Tracking/TrackedPlayer.cs`, mirroring `PlayerDetection`'s shape plus the ID. Define `IPlayerTracker` (a single `Update(IReadOnlyList<PlayerDetection> detections)` returning `IReadOnlyList<TrackedPlayer>`, called once per processed frame per design.md's frame-stepped-not-wall-clock decision) in `src/NBA.Tracking/IPlayerTracker.cs`. Add a `ProjectReference` to `src/NBA.Vision/NBA.Vision.csproj` in `NBA.Tracking.csproj`. Verify the solution builds.

## 2. Motion prediction (per-axis Kalman filters)

- [x] 2.1 Implement `Axis1DKalmanFilter` in `src/NBA.Tracking/Axis1DKalmanFilter.cs`: a minimal position+velocity (2D state) Kalman filter over a single scalar axis, with `Predict()` (advances one frame-step, returns predicted position) and `Correct(double measurement)` (updates state from an observed value), using closed-form 2x2 math per design.md's "no matrix-library dependency" decision. Verify with a unit test in `tests/NBA.Tracking.Tests/Axis1DKalmanFilterTests.cs` that repeated `Predict()`/`Correct()` calls against a synthetic constant-velocity sequence converge toward the true position (not just repeat the last measurement).
- [x] 2.2 Implement `BoxMotionPredictor` in `src/NBA.Tracking/BoxMotionPredictor.cs`: wraps four `Axis1DKalmanFilter`s (one each for box center-x, center-y, width, height), translating a `(Left, Top, Right, Bottom)` box to/from `(cx, cy, w, h)`, exposing `Predict()` (returns the predicted box for the current frame-step) and `Correct(box)` (feeds back an observed box). Verify with a unit test asserting a box moving at constant velocity across several synthetic `Correct` calls produces `Predict()` output that tracks the true trajectory, not a stationary box.

## 3. Greedy IoU association

- [x] 3.1 Implement a standalone IoU helper and `GreedyIouMatcher.Match(predictedBoxes, detectionBoxes, iouThreshold)` in `src/NBA.Tracking/GreedyIouMatcher.cs`, returning matched (predicted-index, detection-index) pairs plus the leftover unmatched indices on both sides — sorting all above-threshold pairs by IoU descending and greedily claiming them (design.md's "greedy, not Hungarian" decision, same pattern `OnnxPlayerDetector.SuppressOverlapping` already uses). Verify with a unit test in `tests/NBA.Tracking.Tests/GreedyIouMatcherTests.cs` using hand-built box sets: overlapping pairs above threshold match, pairs below threshold are left unmatched, and when one predicted box could match two detections, only the higher-IoU pair is kept (the other detection stays unmatched).

## 4. ByteTrack association + lifecycle

- [x] 4.1 Implement `ByteTrackPlayerTracker : IPlayerTracker` in `src/NBA.Tracking/ByteTrackPlayerTracker.cs` with constructor parameters `highConfidenceThreshold = 0.6f`, `highConfidenceIouThreshold = 0.3f`, `lowConfidenceIouThreshold = 0.3f`, `maxLostFrames = 30` (design.md's confidence-tier/IoU/occlusion-buffer decisions). Each `Update` call: (a) `Predict()` every live track's `BoxMotionPredictor`; (b) split input detections into high/low confidence tiers at `highConfidenceThreshold`; (c) round 1 — `GreedyIouMatcher.Match` predicted track boxes against high-confidence detections at `highConfidenceIouThreshold`, `Correct`-ing matched tracks and resetting their lost-frame counter; (d) round 2 — match still-unmatched tracks against low-confidence detections at `lowConfidenceIouThreshold`, same `Correct`/reset on match, discarding any low-confidence detection left unmatched; (e) spawn a new track (new monotonically-increasing ID, starting at 1, never reused) for each high-confidence detection unmatched in round 1; (f) increment the lost-frame counter for every track unmatched in both rounds and drop any track whose counter reaches `maxLostFrames`. Verify the solution builds.
- [x] 4.2 Unit tests in `tests/NBA.Tracking.Tests/ByteTrackPlayerTrackerTests.cs` driving `ByteTrackPlayerTracker` across multiple `Update` calls with hand-built detection sequences, covering: a smoothly moving high-confidence detection keeps the same track ID across frames; a detection that drops to low confidence for one frame (still above `IPlayerDetector`'s own threshold) is matched in round 2 and keeps its track's ID; a low-confidence detection with no nearby existing track is discarded and creates no new track; a track with no matching detection is retained (at its predicted position) for `maxLostFrames - 1` consecutive missed frames and terminated exactly at `maxLostFrames`; an unmatched high-confidence detection creates a new track with a fresh ID; a track ID is never reassigned to a different track after termination, even when a later detection appears at the terminated track's last position.

## 5. Pipeline wiring

- [x] 5.1 Construct a `ByteTrackPlayerTracker` in `src/NBA.App/App.axaml.cs` (no conditional/file-existence check needed, per design.md — always wired) and pass it into `MainWindowViewModel`'s constructor. Verify the app still starts.
- [x] 5.2 Add an `IPlayerTracker` constructor parameter to `MainWindowViewModel`, and in `ProcessFrameArrived` (`src/NBA.App/ViewModels/MainWindowViewModel.cs`) feed `_playerDetector.Detect(...)`'s output through `_playerTracker.Update(...)` before mapping to overlay annotations. Reset the tracker's state on source switch (`SelectSourceAsync`) so track IDs don't persist across an unrelated source change. Verify with a `MainWindowViewModelTests` case that a frame arrival triggers exactly one `Update` call on a test double, and that switching sources resets tracker state.

## 6. Overlay rendering

- [x] 6.1 Replace the current unlabeled foot-point mapping for player detections in `MainWindowViewModel.ProcessFrameArrived` with `OverlayAnnotation.ForBox(t.Left, t.Top, t.Right, t.Bottom, label: $"#{t.TrackId}")` per tracked player. Verify with a `MainWindowViewModelTests` case asserting the raw-view annotation list includes one labeled box annotation per track (label containing the track ID) instead of the previous foot-point annotations.
- [x] 6.2 Verify a frame with zero tracks produces zero player-box annotations (no leftover annotations from a prior frame) — extend the existing frame-arrival test coverage.
