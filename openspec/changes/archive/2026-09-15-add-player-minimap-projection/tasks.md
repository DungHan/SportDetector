## 1. Style-key plumbing through the shared marker/annotation model

- [x] 1.1 Add a `StyleKey` field to `AnnotationVisual` (`src/NBA.App/Models/AnnotationVisual.cs`). Update `AnnotationVisual.FromAnnotation` to pass `annotation.StyleKey` through instead of dropping it, and update `MinimapViewModel.SetMarkers` (`src/NBA.App/ViewModels/MinimapViewModel.cs`) to pass `marker.StyleKey` through the same way. Verify the solution builds and existing `MinimapViewModelTests`/`MainWindowViewModelTests` still pass unmodified.
- [x] 1.2 Add a small `StyleKey`-to-brush converter (e.g. `src/NBA.App/Converters/MarkerStyleToBrushConverter.cs`) mapping `null`/unrecognized → the minimap's current keypoint color (`#40A0FF`), and `"player"` → a new, visually distinct fixed color. Wire it into `MinimapView.axaml`'s marker `DataTemplate` so `Ellipse.Fill` binds through the converter instead of the hardcoded `#40A0FF`. Verify with a unit test on the converter directly (no `StyleKey` → existing color; `"player"` → the new color) — no UI test harness needed for the converter logic itself.

## 2. Project tracked players onto the minimap

- [x] 2.1 In `MainWindowViewModel.ProcessFrameArrived`'s calibrated branch (`src/NBA.App/ViewModels/MainWindowViewModel.cs`, where `keypoints` are currently projected into `markers`), also map each entry of the already-computed `trackedPlayers` to a `CourtMarker`: foot point `ImagePoint((t.Left + t.Right) / 2, t.Bottom)` projected via `PointProjector.Project(calibration, footPoint)`, `Label: $"#{t.TrackId}"`, `StyleKey: "player"`. Pass the keypoint markers and the player markers to `Minimap.SetMarkers(...)` together (single concatenated sequence). Verify the solution builds.
- [x] 2.2 Extend `FrameArrived_WithReusedCalibration_PopulatesBothRawOverlayAndMinimap_FromSameFrame` (`tests/NBA.App.Tests/MainWindowViewModelTests.cs`) to assert `viewModel.Minimap.Markers.Count` equals `keypoints.Count + 1` (the one stubbed tracked player) once tracking is wired through `StubPlayerTracker`, and that the added marker's court-space position matches `PointProjector.Project` applied to the stub detection's foot point — computed the same way the test already computes expected keypoint projections, so the assertion isn't a tautological re-implementation of the production code path.
- [x] 2.3 Add a test asserting a player marker disappears the frame its track disappears: seed a `StubPlayerTracker` with one track under a valid (reused) calibration, publish a frame, assert one player marker is present (`Minimap.Markers.Count` beyond the keypoint markers), then clear `StubPlayerTracker.Tracks` and publish another frame, asserting the minimap's markers drop back to just the keypoint markers — mirroring `FrameArrived_WithNoTracks_ProducesNoLeftoverBoxAnnotationsFromPriorFrame`'s raw-overlay coverage, for the minimap this time.
- [x] 2.4 Add a test asserting no player markers are added when there is no valid calibration: a `StubPlayerTracker` with a non-empty `Tracks` list but no saved/reused calibration for the source, publish a frame, assert `viewModel.Minimap.Markers` stays empty and `HasValidCalibration` is `false` — confirms the existing early-return (before any marker projection) already covers this without extra gating code.
