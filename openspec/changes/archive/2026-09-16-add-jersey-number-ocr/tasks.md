## 1. Test project scaffolding & core types

- [x] 1.1 Create `tests/NBA.JerseyOcr.Tests/NBA.JerseyOcr.Tests.csproj`, mirroring `tests/NBA.Tracking.Tests/NBA.Tracking.Tests.csproj` (xunit + coverlet package references, `Using Include="Xunit"`), with a `ProjectReference` to `src/NBA.JerseyOcr/NBA.JerseyOcr.csproj`. Add the project to `NBA.slnx` under `/tests/`. Verify `dotnet test tests/NBA.JerseyOcr.Tests` runs cleanly (zero tests yet, exit code 0). **Note:** the change's proposal/design assumed `src/NBA.OCR` was an empty scaffold reserved for this work; a concurrently-merged `add-scoreboard-detection`-style change repurposed that exact project for scoreboard text OCR first. This change now lives in a new `src/NBA.JerseyOcr` / `tests/NBA.JerseyOcr.Tests` project pair instead, to avoid colliding with that unrelated capability.
- [x] 1.2 Define `JerseyNumberRecognitionResult` (readonly record struct: `Number` (`int?`, null = unrecognized/no-number), `Confidence` (float)) and `IJerseyNumberRecognizer` (single `Recognize(ReadOnlySpan<byte> bgra8Pixels, int width, int height, int stride, double cropLeft, double cropTop, double cropRight, double cropBottom)` returning `JerseyNumberRecognitionResult`, mirroring `IPlayerDetector`'s pixel-span parameter shape) in `src/NBA.JerseyOcr/JerseyNumberRecognitionResult.cs` / `src/NBA.JerseyOcr/IJerseyNumberRecognizer.cs`. Verify the solution builds.

## 2. Crop preprocessing

- [x] 2.1 Add a crop-rectangle-aware overload of `ImagePreprocessing.ToNchwTensor` in `src/NBA.Inference/ImagePreprocessing.cs` taking `(cropX, cropY, cropWidth, cropHeight, targetWidth, targetHeight)` alongside the existing source pixel/width/height/stride parameters, mapping target pixels into that sub-rectangle instead of the full frame (same nearest-neighbor sampling as the existing overload, reused rather than duplicated). Verify with a unit test in `tests/NBA.Inference.Tests` asserting the cropped overload samples only from within the crop rectangle (e.g. a synthetic image with a distinct color block only inside the crop area produces a uniformly-that-color tensor).
- [x] 2.2 Add a helper (e.g. `CropClamping.Clamp(...)` in `src/NBA.JerseyOcr`) that clamps a `(Left, Top, Right, Bottom)` box to `[0, width) x [0, height)` and reports whether the clamped result has positive width and height, per design.md's degenerate-crop handling. Verify with a unit test covering: a box fully inside bounds is unchanged; a box partially outside bounds is clamped; a box fully outside bounds (or collapsing to zero width/height after clamping) is reported as degenerate.

## 3. Recognizer implementations

- [x] 3.1 Implement `NullJerseyNumberRecognizer : IJerseyNumberRecognizer` in `src/NBA.JerseyOcr/NullJerseyNumberRecognizer.cs`, always returning `JerseyNumberRecognitionResult` with `Number: null`, mirroring `NullPlayerDetector`. Verify with a unit test asserting it always returns unrecognized regardless of input.
- [x] 3.2 Implement `OnnxJerseyNumberRecognizer : IJerseyNumberRecognizer, IDisposable` in `src/NBA.JerseyOcr/OnnxJerseyNumberRecognizer.cs`, using `OnnxModelPipeline` (per design.md's closed-set-classification decision): preprocess clamps the crop (task 2.2), returns unrecognized without invoking the model if the clamped crop is degenerate, otherwise builds the crop tensor (task 2.1) at a configurable `inputSize` (constructor parameter, default e.g. 64); postprocess reads a `[1, 101]`-shaped output (100 number classes + 1 "no number" class, per design.md), taking the argmax class and its softmax-equivalent score as `Confidence`, mapping the "no number" class to `Number: null`. Confidence threshold below which a recognized class is still reported as unrecognized is a constructor parameter (default e.g. 0.5f), matching the existing detector threshold-as-constructor-parameter convention. Verify with a unit test in `tests/NBA.JerseyOcr.Tests` against a small hand-built ONNX fixture model (mirroring `tests/NBA.Vision.Tests/Assets`'s fixture pattern, e.g. `tests/NBA.JerseyOcr.Tests/Assets/jersey-number-fixture.onnx`) covering: a crop that produces a confident number class returns that number; a crop that produces a confident "no number" class (or any class below the confidence threshold) returns unrecognized; a degenerate crop rectangle returns unrecognized without needing the fixture model to produce a specific output.

## 4. Vote aggregation

- [x] 4.1 Define `IJerseyNumberVoteAggregator` (single `Update(IReadOnlyList<(int TrackId, JerseyNumberRecognitionResult Result)> frameResults)` returning `IReadOnlyDictionary<int, int>` — resolved numbers only, keyed by track ID, tracks without a resolved number omitted) and implement `PluralityJerseyNumberVoteAggregator` in `src/NBA.JerseyOcr/IJerseyNumberVoteAggregator.cs` / `src/NBA.JerseyOcr/PluralityJerseyNumberVoteAggregator.cs`, per design.md's plurality-with-threshold decision: each call increments per-track, per-number vote counts for every non-null result in `frameResults`, drops any track ID absent from `frameResults` (discarding its accumulated counts — this is the only termination signal, per design.md), and returns each remaining track's current argmax candidate wherever its count has reached `minVotes` (constructor parameter, default e.g. 5). Verify the solution builds.
- [x] 4.2 Unit tests in `tests/NBA.JerseyOcr.Tests/PluralityJerseyNumberVoteAggregatorTests.cs` driving the aggregator across multiple `Update` calls with hand-built per-track result sequences, covering: a track needs `minVotes` matching results before it resolves (fewer votes, or votes split across candidates with none reaching the threshold, yields no resolved entry for that track); once resolved, a track keeps its resolved number across calls where a different candidate appears but has not yet out-voted it; a different candidate displaces the resolved number once its own count exceeds it (the "overtake" scenario); a track ID absent from a later `Update` call's `frameResults` has its state discarded, and a later call reusing that same track ID (simulating an unrelated new track after ID growth, not real ID reuse) starts accumulating from zero rather than resuming old counts.

## 5. Pipeline wiring

- [x] 5.1 In `src/NBA.App/App.axaml.cs`, add a `jerseyNumberModelPath` (`Path.Combine(GetModelsDirectory(), "jersey-number.onnx")`) and wire `IJerseyNumberRecognizer` = `OnnxJerseyNumberRecognizer` when the file exists, else `NullJerseyNumberRecognizer` (matching the `keypointDetector`/`playerDetector` conditional-wiring pattern). Construct a `PluralityJerseyNumberVoteAggregator` unconditionally (no model dependency, per design.md's "always wired" posture for pure-algorithm components) and pass both into `MainWindowViewModel`'s constructor. Verify the app still starts.
- [x] 5.2 Add `IJerseyNumberRecognizer` and `IJerseyNumberVoteAggregator` constructor parameters to `MainWindowViewModel`. In `ProcessFrameArrived` (`src/NBA.App/ViewModels/MainWindowViewModel.cs`), immediately after computing `trackedPlayers` (unconditionally, before the sport/calibration gates, per design.md), clamp and crop each track's box, call `_jerseyNumberRecognizer.Recognize(...)` per track, and feed the per-track results through `_jerseyNumberVoteAggregator.Update(...)` to get the current frame's resolved-numbers map. Verify with a `MainWindowViewModelTests` case (adding a `StubJerseyNumberRecognizer`/`StubJerseyNumberVoteAggregator` test double pair, mirroring the existing `StubPlayerTracker` pattern) asserting a frame arrival triggers exactly one `Recognize` call per tracked player and exactly one `Update` call on the aggregator.

## 6. Marker label wiring

- [x] 6.1 In `MainWindowViewModel.ProcessFrameArrided`'s minimap-marker construction, use the resolved-numbers map from 5.2 to label each track's `CourtMarker`: `$"#{resolved}"` if that track ID has a resolved number, else the existing `$"#{t.TrackId}"` fallback. Leave the raw-overlay `playerAnnotations` box label untouched (still `$"#{t.TrackId}"`, per design.md's Non-Goals). Verify with `MainWindowViewModelTests` cases asserting: a track with no resolved number still shows its track-ID label on the minimap marker (existing behavior preserved); a track with a resolved number shows that number as the minimap marker's label instead; the raw-overlay box label is unaffected either way.
- [x] 6.2 In `MinimapView.axaml`'s marker `DataTemplate` (`src/NBA.App/Views/MinimapView.axaml`), add a `TextBlock` bound to the marker's `Label`, centered on the existing `Ellipse` (e.g. layered in the same `Canvas` at the same position, centered text). Verify by running the app against a source with tracked players and a valid calibration and visually confirming the label text renders centered on each minimap marker (no automated UI test harness exists for this per the precedent set by the `MarkerStyleToBrushConverter` task in `add-player-minimap-projection`, which verified its converter directly instead).

## 7. Model asset placeholder

- [x] 7.1 Update `models/README.md` to document the expected `jersey-number.onnx` model contract (input: a cropped BGRA8 region resized to `inputSize x inputSize`; output: `[1, 101]` class scores, classes `0`-`99` plus one "no number" class, per design.md), matching how `models/README.md` already documents the other model contracts. No model file is shipped in this change (mirrors `court-keypoints.basketball.onnx`'s and `player-detection.onnx`'s existing placeholder posture) — `NullJerseyNumberRecognizer` remains the real runtime path until one exists.
