## 1. Project scaffolding & core types

- [ ] 1.1 Create `src/NBA.JerseyColor/NBA.JerseyColor.csproj` (mirroring `src/NBA.OCR/NBA.OCR.csproj`'s empty-scaffold shape) and add it to `NBA.slnx` under `/src/`. Create `tests/NBA.JerseyColor.Tests/NBA.JerseyColor.Tests.csproj` (mirroring `tests/NBA.Tracking.Tests/NBA.Tracking.Tests.csproj`: xunit + coverlet references, `Using Include="Xunit"`), with a `ProjectReference` to `src/NBA.JerseyColor/NBA.JerseyColor.csproj`, and add it to `NBA.slnx` under `/tests/`. Verify `dotnet test tests/NBA.JerseyColor.Tests` runs cleanly (zero tests yet, exit code 0).
- [ ] 1.2 Define `JerseyColorExtractionResult` (readonly record struct: `Color` (nullable, e.g. a small `(byte R, byte G, byte B)` struct or `uint?` packed RGB — null means unknown), `Confidence` (float)) and `IJerseyColorExtractor` (single `Extract(ReadOnlySpan<byte> bgra8Pixels, int width, int height, int stride, double cropLeft, double cropTop, double cropRight, double cropBottom)` returning `JerseyColorExtractionResult`, mirroring `IPlayerDetector`'s pixel-span parameter shape and the planned `IJerseyNumberRecognizer`'s crop-rect parameters) in `src/NBA.JerseyColor/JerseyColorExtractionResult.cs` / `src/NBA.JerseyColor/IJerseyColorExtractor.cs`. Verify the solution builds.

## 2. Crop sampling helper

- [ ] 2.1 Add a clamping helper (e.g. `CropClamping.Clamp(...)` in `src/NBA.JerseyColor`) that clamps a `(Left, Top, Right, Bottom)` box to `[0, width) x [0, height)` and reports whether the clamped result has positive width and height, per design.md's degenerate-crop handling (own implementation — deliberately not shared with `add-jersey-number-ocr`, an independent unimplemented sibling change, per design.md's Decisions). Verify with a unit test covering: a box fully inside bounds is unchanged; a box partially outside bounds is clamped; a box fully outside bounds (or collapsing to zero width/height after clamping) is reported as degenerate.
- [ ] 2.2 Add a torso-band helper that narrows a clamped crop rectangle to the fixed fractional sub-rectangle used for sampling (e.g. vertically 15%-55% of box height, full width — constants exposed rather than hardcoded, per design.md). Verify with a unit test asserting the narrowed rectangle's bounds for a few sample input boxes.
- [ ] 2.3 Add a pixel-sampling helper that iterates the BGRA8 pixels within a given rectangle of the source buffer (using `stride` correctly) and returns them for histogram building, without resizing/tensor conversion (per design.md, this capability has no model). Verify with a unit test against a synthetic buffer asserting only pixels within the given rectangle are visited.

## 3. Dominant-color extraction

- [ ] 3.1 Implement quantized-histogram dominant-color computation (e.g. `DominantColorHistogram.Compute(...)` in `src/NBA.JerseyColor`): quantize each sampled pixel to a coarse bucket (e.g. 5 bits per channel, per design.md), count frequency per bucket, return the mode bucket's representative color and a confidence derived from that bucket's share of samples. Verify with a unit test against a synthetic pixel buffer with a clear majority color plus noise pixels, asserting the majority color (or its quantized bucket) is returned.
- [ ] 3.2 Implement `DominantColorJerseyColorExtractor : IJerseyColorExtractor` in `src/NBA.JerseyColor/DominantColorJerseyColorExtractor.cs`, composing tasks 2.1-2.3 and 3.1: clamp the crop, narrow to the torso band, return "unknown" without sampling if the result is degenerate (per design.md), otherwise sample pixels and compute the dominant color. Confidence threshold below which a result is still reported as unknown is a constructor parameter (default e.g. 0.3f), matching the existing detector threshold-as-constructor-parameter convention. Verify with a unit test covering: a crop with a clear dominant color returns that color above the confidence threshold; a crop with no clear majority (or below the confidence threshold) returns unknown; a degenerate crop rectangle returns unknown without sampling.

## 4. Per-track color resolution

- [ ] 4.1 Define `IJerseyColorResolver` (single `Update(IReadOnlyList<(int TrackId, JerseyColorExtractionResult Result)> frameResults)` returning `IReadOnlyDictionary<int, (byte R, byte G, byte B)>` — resolved colors only, keyed by track ID, tracks without a resolved color omitted) and implement `ClusteringJerseyColorResolver` in `src/NBA.JerseyColor/IJerseyColorResolver.cs` / `src/NBA.JerseyColor/ClusteringJerseyColorResolver.cs`, per design.md's distance-based cluster-with-threshold decision: each call buckets a track's non-unknown samples by color-distance proximity (threshold constructor parameter, default e.g. a modest per-channel distance), drops any track ID absent from `frameResults` entirely (discarding its accumulated state — the only termination signal, per design.md), and returns each remaining track's largest bucket's running-average color wherever that bucket's sample count has reached `minSamples` (constructor parameter, default e.g. 5). Verify the solution builds.
- [ ] 4.2 Unit tests in `tests/NBA.JerseyColor.Tests/ClusteringJerseyColorResolverTests.cs` driving the resolver across multiple `Update` calls with hand-built per-track sample sequences, covering: a track needs `minSamples` matching (close-enough) samples before it resolves (fewer samples, or samples spread too far apart, yields no resolved entry for that track); once resolved, a track keeps its resolved color across calls where a differing sample appears but hasn't yet outgrown the winning bucket; a differing color cluster displaces the resolved color once its own bucket's count exceeds the previous winner (the "overtake" scenario); a track ID absent from a later `Update` call's `frameResults` has its state discarded, and a later call reusing that same track ID starts accumulating from zero rather than resuming old state.

## 5. Pipeline wiring

- [ ] 5.1 In `src/NBA.App/App.axaml.cs`, construct a `DominantColorJerseyColorExtractor` and a `ClusteringJerseyColorResolver` unconditionally (no model-file check, per design.md's "no `Null` implementation, no missing-asset state" decision) and pass both into `MainWindowViewModel`'s constructor. Verify the app still starts.
- [ ] 5.2 Add `IJerseyColorExtractor` and `IJerseyColorResolver` constructor parameters to `MainWindowViewModel`. In `ProcessFrameArrived` (`src/NBA.App/ViewModels/MainWindowViewModel.cs`), immediately after computing `trackedPlayers` (unconditionally, before the sport/calibration gates, per design.md — same region as task 5.1's construction targets), call `_jerseyColorExtractor.Extract(...)` per track using each track's box as the crop rectangle, and feed the per-track results through `_jerseyColorResolver.Update(...)` to get the current frame's resolved-colors map. Verify with a `MainWindowViewModelTests` case (adding a `StubJerseyColorExtractor`/`StubJerseyColorResolver` test double pair, mirroring the existing `StubPlayerTracker` pattern) asserting a frame arrival triggers exactly one `Extract` call per tracked player and exactly one `Update` call on the resolver.

## 6. Marker color wiring

- [ ] 6.1 Add a nullable `Color` field (hex string, e.g. `"#RRGGBB"`) to `CourtMarker` (`src/NBA.App/Models/CourtMarker.cs`) and `AnnotationVisual` (`src/NBA.App/Models/AnnotationVisual.cs`), threading it through `AnnotationVisual.FromAnnotation` and `MinimapViewModel.SetMarkers` (`src/NBA.App/ViewModels/MinimapViewModel.cs:89`) alongside the existing `StyleKey`. Verify the solution builds.
- [ ] 6.2 In `MainWindowViewModel.ProcessFrameArrived`'s minimap-marker construction (the `playerMarkers` projection, `src/NBA.App/ViewModels/MainWindowViewModel.cs:220-225`), use the resolved-colors map from 5.2 to set each track's `CourtMarker.Color` (formatted as hex) when resolved, else leave it null. Verify with `MainWindowViewModelTests` cases asserting: a track with no resolved color still produces a marker with `Color: null` (placeholder fill preserved); a track with a resolved color produces a marker with that color's hex string.
- [ ] 6.3 Extend `MarkerStyleToBrushConverter` (`src/NBA.App/Converters/MarkerStyleToBrushConverter.cs`) into an `IMultiValueConverter` accepting `(Color, StyleKey)`: when `Color` is non-null, parse and return it as a `SolidColorBrush`; otherwise fall back to the existing `StyleKey` switch unchanged. Verify with a unit test covering: non-null `Color` returns that color regardless of `StyleKey`; null `Color` with `StyleKey: "player"` returns the existing placeholder; null `Color` with any other `StyleKey` returns the existing keypoint color (both pre-existing behaviors provably unchanged).
- [ ] 6.4 Update `MinimapView.axaml`'s marker `Ellipse.Fill` binding (`src/NBA.App/Views/MinimapView.axaml:34`) to a `MultiBinding` over `Color` and `StyleKey` through the updated converter. Verify by running the app against a source with tracked players and a valid calibration and visually confirming player markers render with the placeholder fill initially and switch to a distinct color once extraction resolves (no automated UI test harness exists for this, per the precedent set in `add-player-minimap-projection`, which verified `MarkerStyleToBrushConverter` directly instead of through the UI).

## 7. Documentation

- [ ] 7.1 Note in `models/README.md` (or a brief comment in `src/NBA.JerseyColor`) that jersey-color detection is classical pixel analysis with no model file, unlike the other capabilities documented there — so a future reader doesn't go looking for a `jersey-color.onnx` asset that was never meant to exist.
