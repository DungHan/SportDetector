## 1. Expose the Kalman filter's corrected estimate

- [x] 1.1 Add a way for `Axis1DKalmanFilter` (`src/NBA.Tracking/Axis1DKalmanFilter.cs`) to report its current `_position` without advancing state (e.g. a `Position` property, or have `Correct` return it) - verify with a unit test that calling `Correct` then reading the estimate returns the same gain-blended value the existing `Predict()`-after-`Correct()` math would compute, without a second `Predict()` call having been made.
- [x] 1.2 Change `BoxMotionPredictor.Correct` (`src/NBA.Tracking/BoxMotionPredictor.cs`) from `void` to returning the corrected `(Left, Top, Right, Bottom)` box, assembled from the four axes' post-correction values - verify with a unit test that the returned box lies between the pre-correction predicted box and the raw measurement box (not equal to the raw measurement).

## 2. Smooth reported player-track boxes

- [x] 2.1 Update `ByteTrackPlayerTracker.ApplyMatch` (`src/NBA.Tracking/ByteTrackPlayerTracker.cs:258-266`) to set `track.LastBox` from `Predictor.Correct(box)`'s return value instead of the raw detection box - verify with a `ByteTrackPlayerTrackerTests` case: feed a track a sequence of detections with small per-frame jitter around a fixed point, and assert the reported box's variance across frames is smaller than the input detections' variance.
- [x] 2.2 Add a `ByteTrackPlayerTrackerTests` case for the `tracking/player-tracking` spec's "steady motion, no visible lag" scenario: feed a track detections moving at a constant velocity across many frames, and assert the reported box's position stays within a small bound of the true (unsmoothed) position at each step, not trailing behind by more than that bound.
- [x] 2.3 Run `dotnet test tests/NBA.Tracking.Tests` and confirm all existing `ByteTrackPlayerTrackerTests` (association, occlusion, confirmation, dedup) still pass unmodified aside from the two additions above - the box *value* reported changes, but every existing assertion about track IDs, confirmation timing, and occlusion lifecycle must be unaffected.

## 3. Add the ball tracker

- [x] 3.1 Add a `BallPosition` record (`Left`, `Top`, `Right`, `Bottom`, `Confidence`) and an `IBallTracker` interface (`Update`/`PredictOnly`/`Reset`, mirroring `IPlayerTracker`'s shape) in `src/NBA.Tracking/` - verify the project builds (`dotnet build src/NBA.Tracking`).
- [x] 3.2 Implement `BallTracker : IBallTracker` using one `BoxMotionPredictor`, a `MissedFrames` counter, an `IsAcquired` flag, and a `maxCoastFrames` constant (documented placeholder, not yet tuned against real footage per design.md's Risks section). `Update` filters its input to `ClassName == "Ball"`, uses only the highest-confidence match if more than one, advances the predictor, and returns a smoothed `BallPosition` while acquired-and-within-coast-bound, or `null` otherwise. `PredictOnly` advances the predictor and returns the same coasted/`null` result without taking any detections. On the first matched detection after exceeding the coast bound, discard the old `BoxMotionPredictor` and construct a fresh one before correcting.
- [x] 3.3 Add `tests/NBA.Tracking.Tests/BallTrackerTests.cs` covering the `tracking/ball-tracking` spec scenarios: single detection updates and smooths the estimate; multiple detections in one attempt use only the highest-confidence one; misses within the coast bound keep reporting a predicted position; misses beyond the bound report `null`; a detection arriving after `null` immediately resumes reporting (no multi-frame confirmation delay) - verify with `dotnet test tests/NBA.Tracking.Tests`.

## 4. Wire the ball tracker into the app

- [x] 4.1 Construct a `BallTracker` in `src/NBA.App/App.axaml.cs` alongside the existing `ByteTrackPlayerTracker` construction (~line 163) and pass it into `MainWindowViewModel`'s constructor - verify `dotnet build src/NBA.App` succeeds.
- [x] 4.2 Add an `IBallTracker _ballTracker` field to `MainWindowViewModel` (mirroring `_playerTracker`), call `_ballTracker.Update(...)` on detection frames (same `isDetectionFrame` gate and cadence as `_playerTracker.Update(...)`, using this frame's `Others`/ball-detection result) and `_ballTracker.PredictOnly()` otherwise, and call `_ballTracker.Reset()` everywhere `_playerTracker.Reset()` is already called (source switch, calibration invalidation) - verify by reading the diff against `src/NBA.App/ViewModels/MainWindowViewModel.cs:734-757` and `:220`, `:545` for parity with the player-tracker call sites.
- [x] 4.3 Re-point the `Ball` raw-view annotation and the minimap `ballMarker` (`src/NBA.App/ViewModels/MainWindowViewModel.cs:909-918` and the `otherAnnotations` construction) to read from `_ballTracker`'s latest returned `BallPosition?` instead of scanning `_lastOtherDetections` for `ClassName == "Ball"`; leave `_lastOtherDetections` and every other non-`Player` class's rendering untouched.
- [x] 4.4 Extend `tests/NBA.App.Tests/MainWindowViewModelTests.cs` with a case for the `vision/on-court-object-detection` spec's coast-through-a-missed-frame scenario: publish a detection frame with a `Ball` detection, then a later detection frame with none (within the coast bound), and assert the raw-view/minimap ball marker is still rendered at the coasted position rather than disappearing - verify with `dotnet test tests/NBA.App.Tests`.

## 5. Full verification

- [x] 5.1 Run the full test suite (`dotnet test`) and confirm everything passes. (`NBA.Capture.Tests.MacFrameSourceSmokeTests` has 4 pre-existing failures unrelated to this change - confirmed by re-running them on `main` before this change's edits, same failures - they need a real display/window capture this sandbox doesn't provide. Every other project passes.)
- [ ] 5.2 Run the app against a sample source and visually confirm both the player boxes and the ball marker track smoothly with materially less per-frame jitter than before, and that the ball marker survives at least one isolated missed-detection frame without disappearing.
