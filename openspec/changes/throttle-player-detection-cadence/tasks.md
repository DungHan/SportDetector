## 1. Tracker: predict-only advancement

- [x] 1.1 In `src/NBA.Tracking/ByteTrackPlayerTracker.cs`, factor the existing predict-step loop (currently the first block inside `Update`, lines 31-35: advance each track's `Predictor.Predict()` and set `LastBox` to the predicted box) into a private helper method (e.g. `AdvancePredictions()`) with no other behavior change. Verify existing `ByteTrackPlayerTrackerTests` still pass unmodified.
- [x] 1.2 Add `PredictOnly()` to `IPlayerTracker` (`src/NBA.Tracking/IPlayerTracker.cs`) with a doc comment describing it as the no-detection-attempted counterpart to `Update`: advances motion prediction one frame-step and returns current tracks' boxes, without association, without touching `LostFrames`, without spawning or terminating tracks. Implement it in `ByteTrackPlayerTracker` using the helper from 1.1, returning `_tracks.Select(t => new TrackedPlayer(...))` the same way `Update` does at its end. Verify the solution builds.
- [x] 1.3 Unit tests in `tests/NBA.Tracking.Tests/ByteTrackPlayerTrackerTests.cs` covering: (a) a live track's box moves toward its predicted position after one `PredictOnly()` call and its track ID is unchanged; (b) calling `PredictOnly()` repeatedly for more than `maxLostFrames` consecutive calls does NOT terminate the track (unlike the existing `Update([])`-driven occlusion-timeout test), proving `PredictOnly` doesn't age `LostFrames`; (c) `PredictOnly()` with zero live tracks returns an empty list without throwing; (d) a track that goes unmatched under real `Update` calls still terminates at exactly `maxLostFrames` consecutive unmatched `Update` calls even when `PredictOnly()` calls are interleaved between them (proving the two counters - detection attempts vs. frames - stay independent).

## 2. Detection cadence in the pipeline

- [x] 2.1 Add a cadence constructor parameter to `MainWindowViewModel` (e.g. `int detectionIntervalFrames = 3`, following this codebase's existing constructor-parameter-with-default convention for unvalidated thresholds) and a private frame counter field. In `OnFrameArrived` (`src/NBA.App/ViewModels/MainWindowViewModel.cs:151-175`), branch on the counter: when it's a detection frame, keep the existing `_playerDetector.Detect(...)` + `_playerTracker.Update(...)` sequence (including the existing try/catch around `Detect`); otherwise call `_playerTracker.PredictOnly()` and skip `Detect` entirely. Increment the counter once per call regardless of branch. Verify the solution builds.
- [x] 2.2 Reset the frame counter to 0 in `SelectSourceAsync` (`src/NBA.App/ViewModels/MainWindowViewModel.cs:113`) alongside the existing `_playerTracker.Reset()` call, so a fresh source's first frame is always a real detection frame. Verify with a `MainWindowViewModelTests` case that the very first frame after `SelectSourceAsync` triggers `_playerDetector.Detect` on a test double even if a prior source had left the counter mid-cycle.
- [x] 2.3 Unit tests in `tests/NBA.App.Tests/MainWindowViewModelTests.cs`: with `detectionIntervalFrames = 3`, feeding 3 consecutive frames through `OnFrameArrived` triggers `_playerDetector.Detect` exactly once (on the first) and `_playerTracker.PredictOnly()` exactly twice (on the second and third), never `Update` on the skipped frames.

## 3. Doc comments

- [x] 3.1 Update the doc comments that currently assert per-frame-always semantics to describe the new cadence-gated behavior: `IPlayerDetector.cs:3` ("runs on every captured frame"), `IPlayerTracker.cs:8` ("Called exactly once per processed frame"), and the `ByteTrackPlayerTracker` type-level comment's framing of `Update`'s frame-stepping, noting `PredictOnly` as the sibling call used on non-detection frames. No behavior change; verify by reading the diff for accuracy against the new spec deltas.
